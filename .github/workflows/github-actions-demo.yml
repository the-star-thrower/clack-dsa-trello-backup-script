name: Trello Board Backup
run-name: Trello Board Backup #${{ github.run_number }} 🌹
on: [push]
  #workflow_dispatch:
  #schedule:
  #- cron: '00 08 * * *' # every day at 08:00 UTC / 00:00 PT
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      #- name: Checkout
      #  uses: the-star-thrower/clack-dsa-trello-backup-script@v0
      - name: run Trello REST API script w/ secrets
        env:
          API_KEY: ${{ secrets.API_KEY }}
          TOKEN: ${{ secrets.TOKEN }}
          BOARD_ID: ${{ secrets.BOARD_ID }}
        run: |
          bash ${{ github.workspace }}/board-export-json.sh
          #echo "ran board backup script"
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: Run script
        run: |
          bash ${GITHUB_WORKSPACE}/board-export-json.sh
          bash ${{ github.workspace }}
      - run: echo "Setup api key and token to address 'invalid key⏎' output"
        shell: bash
      - run: echo "🍏 This job's status is ${{ job.status }}."
